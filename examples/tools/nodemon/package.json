{
  "name": "nodemon-example",
  "version": "1.0.0",
  "scripts": {
    "start": "nodemon -e js,yml ./node_modules/.bin/serverless offline start",
    "start:alt": "nodemon --exec serverless offline start -e js,yml"
  },
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.1",
    "serverless": "^1.58.0",
    "serverless-offline": "^12.0.4"
  }
}
